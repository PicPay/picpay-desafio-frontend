import { TransactionResponse } from '@models/transactionResponse/transaction-response.model';

describe('TransactionResponse', () => {
  it('should create an instance', () => {
    expect(new TransactionResponse()).toBeTruthy();
  });
});
