import { TransactionResponseEntity } from './transaction-response-entity';

describe('TransactionResponseEntity', () => {
  it('should create an instance', () => {
    expect(new TransactionResponseEntity()).toBeTruthy();
  });
});
