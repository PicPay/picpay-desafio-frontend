import { TransactionPayloadEntity } from './transaction-payload-entity';

describe('TransactionPayloadEntity', () => {
  it('should create an instance', () => {
    expect(new TransactionPayloadEntity()).toBeTruthy();
  });
});
