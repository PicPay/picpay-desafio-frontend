import { MainColorDirective } from './main-color.directive';

describe('MainColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MainColorDirective();
    expect(directive).toBeTruthy();
  });
});
