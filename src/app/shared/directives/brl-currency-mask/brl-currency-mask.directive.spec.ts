import { BrlCurrencyMaskDirective } from './brl-currency-mask.directive';

describe('BrlCurrencyMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new BrlCurrencyMaskDirective();
    expect(directive).toBeTruthy();
  });
});
